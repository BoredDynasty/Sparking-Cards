--!strict
--!native
--!optimize 2
--!nolint LocalShadow
--#selene: allow(shadowing)
-- File generated by Blink v0.18.6 (https://github.com/1Axen/Blink)
-- This file is not meant to be edited

_G._BLINK = _G._BLINK or {}
if _G._BLINK["BLINK"] then
	error(
		'[Blink]: An instance of blink is already running with the remote scope "BLINK". Change the remote scope of either instance to avoid conflicts.'
	)
end
_G._BLINK["BLINK"] = true
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")

local Future = require(ReplicatedStorage.Packages.future)
local BASE_EVENT_NAME = "BLINK"
local Invocations = 0

local SendSize = 64
local SendOffset = 0
local SendCursor = 0
local SendBuffer = buffer.create(64)
local SendInstances = {}

local RecieveCursor = 0
local RecieveBuffer = buffer.create(64)

local RecieveInstances = {}
local RecieveInstanceCursor = 0

local Null = newproxy()

type Entry = {
	value: any,
	next: Entry?,
}

type Queue = {
	head: Entry?,
	tail: Entry?,
}

type BufferSave = {
	Size: number,
	Cursor: number,
	Buffer: buffer,
	Instances: { Instance },
}

local function Read(Bytes: number)
	local Offset = RecieveCursor
	RecieveCursor += Bytes
	return Offset
end

local function Save(): BufferSave
	return {
		Size = SendSize,
		Cursor = SendCursor,
		Buffer = SendBuffer,
		Instances = SendInstances,
	}
end

local function Load(Save: BufferSave?)
	if Save then
		SendSize = Save.Size
		SendCursor = Save.Cursor
		SendOffset = Save.Cursor
		SendBuffer = Save.Buffer
		SendInstances = Save.Instances
		return
	end

	SendSize = 64
	SendCursor = 0
	SendOffset = 0
	SendBuffer = buffer.create(64)
	SendInstances = {}
end

local function Invoke()
	if Invocations == 255 then
		Invocations = 0
	end

	local Invocation = Invocations
	Invocations += 1
	return Invocation
end

local function Allocate(Bytes: number)
	local InUse = (SendCursor + Bytes)
	if InUse > SendSize then
		--> Avoid resizing the buffer for every write
		while InUse > SendSize do
			SendSize *= 1.5
		end

		local Buffer = buffer.create(SendSize)
		buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
		SendBuffer = Buffer
	end

	SendOffset = SendCursor
	SendCursor += Bytes

	return SendOffset
end

local function CreateQueue(): Queue
	return {
		head = nil,
		tail = nil,
	}
end

local function Pop(queue: Queue): any
	local head = queue.head
	if head == nil then
		return
	end

	queue.head = head.next
	return head.value
end

local function Push(queue: Queue, value: any)
	local entry: Entry = {
		value = value,
		next = nil,
	}

	if queue.tail ~= nil then
		queue.tail.next = entry
	end

	queue.tail = entry

	if queue.head == nil then
		queue.head = entry
	end
end

local Calls = table.create(256)

local Events: any = {
	Reliable = table.create(256),
	Unreliable = table.create(256),
}

local Queue: any = {
	Reliable = table.create(256),
	Unreliable = table.create(256),
}

local SerdesEvent = "?"
local SerdesThread: thread?
Events.Reliable[0] = {}
Queue.Reliable[0] = table.create(256)
Events.Reliable[1] = {}
Queue.Reliable[1] = table.create(256)
Events.Reliable[4] = {}
Queue.Reliable[4] = table.create(256)
Events.Reliable[5] = {}
Queue.Reliable[5] = table.create(256)
Events.Reliable[8] = {}
Queue.Reliable[8] = table.create(256)
Events.Reliable[9] = {}
Queue.Reliable[9] = table.create(256)
Events.Reliable[10] = {}
Queue.Reliable[10] = table.create(256)
Events.Reliable[11] = {}
Queue.Reliable[11] = table.create(256)
Events.Reliable[12] = {}
Queue.Reliable[12] = table.create(256)
Events.Reliable[13] = {}
Queue.Reliable[13] = table.create(256)
Events.Reliable[14] = {}
Queue.Reliable[14] = table.create(256)
Events.Reliable[15] = {}
Queue.Reliable[15] = table.create(256)
export type Player = Player
export type str_to_int = { [string]: number }
export type Profile = {
	Data: {
		Cards: { [string]: number },
		Prisms: number,
		Level: number,
		Experience: number,
		EquippedCard: string,
		victories: number,
		emotes: { i8: { name: string, identification: number } },
		options: { string: any },
		["equipped_emotes"]: { number },
		achievements: {
			i8: { name: string, description: string, points: number },
			quests: {
				i8: {
					name: string,
					description: string,
					["inquirer_img"]: any,
					progress: number,
					["date_acquired"]: number,
					["date_completed"]: number?,
					module: Instance?,
				},
			},
			products: { i16: { id: number, ["reciept_info"]: any, ["times_bought"]: { [string]: number } } },
			keybinds: { [string]: number },
		},
	},
}
local function ReadEVENT_CreateMatch(): Player
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	if typeof(Value) ~= "Instance" then
		error(`Expected an Instance, got {typeof(Value)} instead.`)
	end
	if not Value:IsA("Player") then
		error(`Expected an Instance of type "Player", got "{Value.ClassName}" instead.`)
	end
	return Value
end

local function ReadEVENT_CancelMatch(): Player
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	if typeof(Value) ~= "Instance" then
		error(`Expected an Instance, got {typeof(Value)} instead.`)
	end
	if not Value:IsA("Player") then
		error(`Expected an Instance of type "Player", got "{Value.ClassName}" instead.`)
	end
	return Value
end

local function WriteEVENT_StreamArea(Value: Vector3): ()
	-- Allocate BLOCK: 13 bytes
	local BLOCK_START = Allocate(13)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 2)
	local Vector = Value
	buffer.writef32(SendBuffer, BLOCK_START + 1, Vector.X)
	buffer.writef32(SendBuffer, BLOCK_START + 5, Vector.Y)
	buffer.writef32(SendBuffer, BLOCK_START + 9, Vector.Z)
end

local function WriteEVENT_PurchaseCard(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 3)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

local function ReadEVENT_RewardExperience(): number
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Value = buffer.readi16(RecieveBuffer, BLOCK_START + 0)
	return Value
end

local function ReadEVENT_AscendRank(): number
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readi8(RecieveBuffer, BLOCK_START + 0)
	return Value
end

local function WriteEVENT_SellCard(Value: string): ()
	-- Allocate BLOCK: 3 bytes
	local BLOCK_START = Allocate(3)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 6)
	local Length = #Value
	buffer.writeu16(SendBuffer, BLOCK_START + 1, Length)
	Allocate(Length)
	buffer.writestring(SendBuffer, SendOffset, Value, Length)
end

local function WriteEVENT_PayPrisms(Value: number): ()
	-- Allocate BLOCK: 5 bytes
	local BLOCK_START = Allocate(5)
	buffer.writeu8(SendBuffer, BLOCK_START + 0, 7)
	buffer.writei32(SendBuffer, BLOCK_START + 1, Value)
end

local function ReadEVENT_ReplicateAnimation(): string
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_CancelAnimation(): string
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_StopAllAnimations(): Player
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	if typeof(Value) ~= "Instance" then
		error(`Expected an Instance, got {typeof(Value)} instead.`)
	end
	if not Value:IsA("Player") then
		error(`Expected an Instance of type "Player", got "{Value.ClassName}" instead.`)
	end
	return Value
end

local function ReadEVENT_DeductStamina(): number
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readi8(RecieveBuffer, BLOCK_START + 0)
	if Value < 1 then
		error(`Expected "Value" to be larger than 1, got {Value} instead.`)
	end
	if Value > 100 then
		error(`Expected "Value" to be smaller than 100, got {Value} instead.`)
	end
	return Value
end

local function ReadEVENT_AddStamina(): number
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readi8(RecieveBuffer, BLOCK_START + 0)
	if Value < 0 then
		error(`Expected "Value" to be larger than 0, got {Value} instead.`)
	end
	if Value > 100 then
		error(`Expected "Value" to be smaller than 100, got {Value} instead.`)
	end
	return Value
end

local function ReadEVENT_SetNetworkOwner(): Instance
	RecieveInstanceCursor += 1
	local Value = RecieveInstances[RecieveInstanceCursor]
	if typeof(Value) ~= "Instance" then
		error(`Expected an Instance, got {typeof(Value)} instead.`)
	end
	if not Value:IsA("Instance") then
		error(`Expected an Instance of type "Instance", got "{Value.ClassName}" instead.`)
	end
	return Value
end

local function ReadEVENT_AddQuest(): string
	-- Read BLOCK: 2 bytes
	local BLOCK_START = Read(2)
	local Length = buffer.readu16(RecieveBuffer, BLOCK_START + 0)
	local Value = buffer.readstring(RecieveBuffer, Read(Length), Length)
	return Value
end

local function ReadEVENT_SetTime(): number
	-- Read BLOCK: 1 bytes
	local BLOCK_START = Read(1)
	local Value = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	if Value < 0 then
		error(`Expected "Value" to be larger than 0, got {Value} instead.`)
	end
	if Value > 24 then
		error(`Expected "Value" to be smaller than 24, got {Value} instead.`)
	end
	return Value
end

if not RunService:IsRunning() then
	local NOOP = function() end
	local Returns = table.freeze({
		CreateMatch = {
			on = NOOP,
		},
		CancelMatch = {
			on = NOOP,
		},
		StreamArea = {
			fire = NOOP,
		},
		PurchaseCard = {
			fire = NOOP,
		},
		RewardExperience = {
			on = NOOP,
		},
		AscendRank = {
			on = NOOP,
		},
		SellCard = {
			fire = NOOP,
		},
		PayPrisms = {
			fire = NOOP,
		},
		ReplicateAnimation = {
			on = NOOP,
		},
		CancelAnimation = {
			on = NOOP,
		},
		StopAllAnimations = {
			on = NOOP,
		},
		DeductStamina = {
			on = NOOP,
		},
		AddStamina = {
			on = NOOP,
		},
		SetNetworkOwner = {
			on = NOOP,
		},
		AddQuest = {
			on = NOOP,
		},
		SetTime = {
			on = NOOP,
		},
	})
	return Returns :: BLINK_EVENTS_SYMBOL
end

if not RunService:IsClient() then
	error("Client network module can only be required from the client.")
end

local Reliable: RemoteEvent =
	ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_RELIABLE_REMOTE") :: RemoteEvent
local Unreliable: UnreliableRemoteEvent =
	ReplicatedStorage:WaitForChild(BASE_EVENT_NAME .. "_UNRELIABLE_REMOTE") :: UnreliableRemoteEvent

local function StepReplication()
	if SendCursor <= 0 then
		return
	end

	local Buffer = buffer.create(SendCursor)
	buffer.copy(Buffer, 0, SendBuffer, 0, SendCursor)
	Reliable:FireServer(Buffer, SendInstances)

	SendSize = 64
	SendCursor = 0
	SendOffset = 0
	SendBuffer = buffer.create(64)
	table.clear(SendInstances)
end
RunService.Heartbeat:Connect(StepReplication)
Reliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: { Instance })
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(
			`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`
		)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while RecieveCursor < Size do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
		if Index == 0 then
			local Value: Player = ReadEVENT_CreateMatch()
			if #Events.Reliable[0] > 0 then
				for _, Listener in Events.Reliable[0] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[0] > 256 then
					warn(
						'[Blink]: Event queue of "CreateMatch" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[0], { Value } :: { any })
			end
		elseif Index == 1 then
			local Value: Player = ReadEVENT_CancelMatch()
			if #Events.Reliable[1] > 0 then
				for _, Listener in Events.Reliable[1] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[1] > 256 then
					warn(
						'[Blink]: Event queue of "CancelMatch" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[1], { Value } :: { any })
			end
		elseif Index == 4 then
			local Value: number = ReadEVENT_RewardExperience()
			if #Events.Reliable[4] > 0 then
				for _, Listener in Events.Reliable[4] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[4] > 256 then
					warn(
						'[Blink]: Event queue of "RewardExperience" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[4], { Value } :: { any })
			end
		elseif Index == 5 then
			local Value: number = ReadEVENT_AscendRank()
			if #Events.Reliable[5] > 0 then
				for _, Listener in Events.Reliable[5] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[5] > 256 then
					warn(
						'[Blink]: Event queue of "AscendRank" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[5], { Value } :: { any })
			end
		elseif Index == 8 then
			local Value: string = ReadEVENT_ReplicateAnimation()
			if #Events.Reliable[8] > 0 then
				for _, Listener in Events.Reliable[8] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[8] > 256 then
					warn(
						'[Blink]: Event queue of "ReplicateAnimation" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[8], { Value } :: { any })
			end
		elseif Index == 9 then
			local Value: string = ReadEVENT_CancelAnimation()
			if #Events.Reliable[9] > 0 then
				for _, Listener in Events.Reliable[9] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[9] > 256 then
					warn(
						'[Blink]: Event queue of "CancelAnimation" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[9], { Value } :: { any })
			end
		elseif Index == 10 then
			local Value: Player = ReadEVENT_StopAllAnimations()
			if #Events.Reliable[10] > 0 then
				for _, Listener in Events.Reliable[10] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[10] > 256 then
					warn(
						'[Blink]: Event queue of "StopAllAnimations" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[10], { Value } :: { any })
			end
		elseif Index == 11 then
			local Value: number = ReadEVENT_DeductStamina()
			if #Events.Reliable[11] > 0 then
				for _, Listener in Events.Reliable[11] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[11] > 256 then
					warn(
						'[Blink]: Event queue of "DeductStamina" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[11], { Value } :: { any })
			end
		elseif Index == 12 then
			local Value: number = ReadEVENT_AddStamina()
			if #Events.Reliable[12] > 0 then
				for _, Listener in Events.Reliable[12] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[12] > 256 then
					warn(
						'[Blink]: Event queue of "AddStamina" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[12], { Value } :: { any })
			end
		elseif Index == 13 then
			local Value: Instance = ReadEVENT_SetNetworkOwner()
			if #Events.Reliable[13] > 0 then
				for _, Listener in Events.Reliable[13] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[13] > 256 then
					warn(
						'[Blink]: Event queue of "SetNetworkOwner" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[13], { Value } :: { any })
			end
		elseif Index == 14 then
			local Value: string = ReadEVENT_AddQuest()
			if #Events.Reliable[14] > 0 then
				for _, Listener in Events.Reliable[14] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[14] > 256 then
					warn(
						'[Blink]: Event queue of "AddQuest" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[14], { Value } :: { any })
			end
		elseif Index == 15 then
			local Value: number = ReadEVENT_SetTime()
			if #Events.Reliable[15] > 0 then
				for _, Listener in Events.Reliable[15] do
					task.spawn(Listener, Value)
				end
			else
				if #Queue.Reliable[15] > 256 then
					warn(
						'[Blink]: Event queue of "SetTime" exceeded 256, did you forget to implement a listener?'
					)
				end
				table.insert(Queue.Reliable[15], { Value } :: { any })
			end
		end
	end
end)
Unreliable.OnClientEvent:Connect(function(Buffer: buffer, Instances: { Instance })
	RecieveCursor = 0
	RecieveBuffer = Buffer
	RecieveInstances = Instances
	RecieveInstanceCursor = 0
	local Size = buffer.len(RecieveBuffer)
	if SerdesThread and coroutine.status(SerdesThread) == "suspended" then
		warn(
			`[Blink]: Yielded in a Sync call. Event "{SerdesEvent}" uses a Sync call, consider using an Async call instead.`
		)
		coroutine.close(SerdesThread)
	end
	SerdesThread = coroutine.running()
	while RecieveCursor < Size do
		-- Read BLOCK: 1 bytes
		local BLOCK_START = Read(1)
		local Index = buffer.readu8(RecieveBuffer, BLOCK_START + 0)
	end
end)

local Returns = table.freeze({
	step_replication = StepReplication,

	CreateMatch = {
		on = function(Listener: (Value: Player) -> ()): () -> ()
			table.insert(Events.Reliable[0], Listener)
			for Index, Arguments in Queue.Reliable[0] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[0] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[0], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[0], Index)
				end
			end
		end,
	},
	CancelMatch = {
		on = function(Listener: (Value: Player) -> ()): () -> ()
			table.insert(Events.Reliable[1], Listener)
			for Index, Arguments in Queue.Reliable[1] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[1] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[1], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[1], Index)
				end
			end
		end,
	},
	StreamArea = {
		fire = function(Value: Vector3): ()
			WriteEVENT_StreamArea(Value)
		end,
	},
	PurchaseCard = {
		fire = function(Value: string): ()
			WriteEVENT_PurchaseCard(Value)
		end,
	},
	RewardExperience = {
		on = function(Listener: (Value: number) -> ()): () -> ()
			table.insert(Events.Reliable[4], Listener)
			for Index, Arguments in Queue.Reliable[4] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[4] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[4], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[4], Index)
				end
			end
		end,
	},
	AscendRank = {
		on = function(Listener: (Value: number) -> ()): () -> ()
			table.insert(Events.Reliable[5], Listener)
			for Index, Arguments in Queue.Reliable[5] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[5] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[5], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[5], Index)
				end
			end
		end,
	},
	SellCard = {
		fire = function(Value: string): ()
			WriteEVENT_SellCard(Value)
		end,
	},
	PayPrisms = {
		fire = function(Value: number): ()
			WriteEVENT_PayPrisms(Value)
		end,
	},
	ReplicateAnimation = {
		on = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[8], Listener)
			for Index, Arguments in Queue.Reliable[8] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[8] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[8], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[8], Index)
				end
			end
		end,
	},
	CancelAnimation = {
		on = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[9], Listener)
			for Index, Arguments in Queue.Reliable[9] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[9] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[9], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[9], Index)
				end
			end
		end,
	},
	StopAllAnimations = {
		on = function(Listener: (Value: Player) -> ()): () -> ()
			table.insert(Events.Reliable[10], Listener)
			for Index, Arguments in Queue.Reliable[10] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[10] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[10], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[10], Index)
				end
			end
		end,
	},
	DeductStamina = {
		on = function(Listener: (Value: number) -> ()): () -> ()
			table.insert(Events.Reliable[11], Listener)
			for Index, Arguments in Queue.Reliable[11] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[11] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[11], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[11], Index)
				end
			end
		end,
	},
	AddStamina = {
		on = function(Listener: (Value: number) -> ()): () -> ()
			table.insert(Events.Reliable[12], Listener)
			for Index, Arguments in Queue.Reliable[12] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[12] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[12], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[12], Index)
				end
			end
		end,
	},
	SetNetworkOwner = {
		on = function(Listener: (Value: Instance) -> ()): () -> ()
			table.insert(Events.Reliable[13], Listener)
			for Index, Arguments in Queue.Reliable[13] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[13] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[13], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[13], Index)
				end
			end
		end,
	},
	AddQuest = {
		on = function(Listener: (Value: string) -> ()): () -> ()
			table.insert(Events.Reliable[14], Listener)
			for Index, Arguments in Queue.Reliable[14] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[14] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[14], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[14], Index)
				end
			end
		end,
	},
	SetTime = {
		on = function(Listener: (Value: number) -> ()): () -> ()
			table.insert(Events.Reliable[15], Listener)
			for Index, Arguments in Queue.Reliable[15] do
				task.spawn(Listener, table.unpack(Arguments))
			end
			Queue.Reliable[15] = {}
			return function(): ()
				local Index = table.find(Events.Reliable[15], Listener)
				if Index ~= nil then
					table.remove(Events.Reliable[15], Index)
				end
			end
		end,
	},
})
type BLINK_EVENTS_SYMBOL = typeof(Returns)
return Returns :: BLINK_EVENTS_SYMBOL
